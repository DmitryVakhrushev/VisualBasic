Module Module1

    Sub Main()
        Dim num1 As Integer = 5
        Dim num2 As Integer = 3
        Dim answer As Double = Nothing 'instead o leaving empty

        answer = num1 / num2 ' we can use *,/,+,-,^, MOD

        Console.WriteLine(answer)
        Console.ReadLine()

    End Sub

End Module
