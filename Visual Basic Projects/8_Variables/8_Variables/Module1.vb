Module Module1

    Sub Main()

        Dim Var As Integer 'just to declare a variable

        Dim myNum As Integer = 5 'defining a variable myNum
        Dim myString As String = "Hello thenewboston fans!"
        Dim myChar As Char = "A"
        Dim myD As Double = 5.934
        Dim myBoo As Boolean = True 'or False

        Console.WriteLine(myNum)
        Console.WriteLine(myString)
        Console.WriteLine(myChar)
        Console.WriteLine(myD)
        Console.WriteLine(myBoo)

        Dim conv As Integer = 135
        Console.WriteLine(conv.ToString())

        Console.ReadLine()
    End Sub

End Module
